// const filePath = path.join(__dirname, `../../../data/${language}`)
// fs.readdir(filePath, 'utf-8', async (err, res) => {
//     // 1. 若不存在 language 同名文件夹
//     if (err) {
//       // 1.1. 创建 language 同名文件夹
//       await createFolder(filePath)
//       // 1.2 创建以及初始化所有文件
//       await writeFile(filePath_wordDetail, JSON.stringify(wordDetailToSave))
//     }
//   })
